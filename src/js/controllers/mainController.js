(function (controllerId) {
  'use strict';

  var controller = function($scope, parseInputService) {

  };

controller.$inject = [
  '$scope',
  'parseInputService'
];

angular.module('app').controller(controllerId, controller);

})('app.mainController');